public class ComplejidadLineal {
    // ---------------------------------------------
    // O(n) – Tiempo lineal
    // ---------------------------------------------
    public static void lineal(int[] arr) {
        for (int num : arr) {
            System.out.println(num);
        }
    }
}
